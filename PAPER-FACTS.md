# PAPER-FACTS — <authors, year, short title>   [skeleton by intake]
# This file reflects the CURRENT understanding of what the paper states.
# Agent-editable for material corrections only; not a history.
#
#   FACT-<id> | claim | source (page/table/eq + provenance) | status
#     provenance: where a row was image-checked, note it
#       (image-verified | image-reconstructed — extraction garbled,
#       rebuilt from the page image)
#     status: NEEDED -> FILLED | UNDERSPECIFIED |
#       UNDERSPECIFIED-EXTRACTION (extraction is ambiguous or
#       unresolved against the source page; resolved at sign-off)
#
# Verification is TARGETED: inspect the rasterized page (refs/pages/)
# wherever parsing, layout, equations, tables, or figures make the
# extracted reading untrustworthy, or a load-bearing number's parsed
# form is doubtful — not as a blanket per-row requirement. The PDF is
# the anchor, refs/paper.md only a reading aid; on any disagreement
# the page image wins. A degraded (non-Marker) extraction lowers the
# threshold for suspicion; it does not make verification blanket.
#
# Corrections: when evidence discovered during implementation shows a
# row was misread, update the row in place and append to it:
#   (corrected <date>: was "<old reading>" — <located quote/page that
#   shows the correction>)
# Material corrections only. A correction that changes what a band or
# gate criterion MEANS is stop-and-ask; otherwise correct, note,
# continue. This is normal operation, not a process failure.
#
# The intake skeleton: one section per pillar; every number the final
# parity report will score appears as a FACT row. Quote-located, never
# from memory. Paper silence is a finding (UNDERSPECIFIED), not a guess.

(empty — skeleton written at intake)
