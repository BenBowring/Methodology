# GOAL — written by the human before intake. One paragraph + the stubs.

## The goal
<One paragraph. Be explicit about the outcome destination. Examples of the
form: "Replicate this model to ballpark-defensible parity so I can extend
it in phase B" / "Reproduce the underlying dataset in this paper so the
space is open to me going forward" / "Rebuild this pipeline as a trusted
baseline for systematic improvement.">

## What done looks like
<One or two sentences. The intake compiler will sharpen this into
ACCEPTABILITY.md and force choices you haven't made — but seed it.>

## Known constraints
<Compute (local/cloud), data access limits, deadlines, anything the paper
depends on that you already know is unavailable.>

## Out of scope
<What the agent must NOT drift into, if you already know.>
