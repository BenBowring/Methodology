# ACCEPTABILITY — what done means for THIS project. Written by the intake
# compiler from refs/GOAL.md, frozen at sign-off, amendable only by
# ruling. Phase-gate claims are quoted verbatim from here.
#
# Contains, when compiled:
# - Replication standard (default: ballpark-defensible — sensibly against
#   published results, not exact, divergences attributed).
# - THE LOAD-BEARING CLAIM SET: the pillar claims. These are the only
#   claims eligible for parity recovery if they fail blind; failed
#   supporting rows are reported as-is, never auto-promoted.
# - Per pillar: metric | band (+ its loosening ceiling, in the metric's
#   own units and direction) | evidence form that counts (parity row,
#   coverage test, holdout; a preprocessing-sensitivity range where the
#   result is known to depend materially on defensible preprocessing
#   choices). Weighted scoring only if this project explicitly needs
#   it — say so here if so.
# - Dual reporting: every scored claim reports BLIND and (where
#   recovery ran) RECOVERED side by side; the blind value is never
#   replaced. Top-level: blind parity rate, recovered parity rate,
#   unresolved rate.
# - Phase claims: the one-sentence claim each phase gate rules on.
# - THE ABANDONMENT LINE: the explicit condition under which the human
#   would abandon this replication.
#
(empty — written by the intake milestone)
