# IMPROVEMENT-BACKLOG — anything out of scope for the current phase.
# Model-shaped ideas during a data phase, strengthening passes, template
# process changes (these get applied to the TEMPLATE, never patched into
# this running clone), and experiment-branch candidates.
#
# Format: - [B-<n>] <one line> | origin (milestone/ruling) | destination
#   (next phase | template | experiment branch)
#
(empty)
