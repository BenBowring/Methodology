# KNOWN-WEAK — items that are incorrect-but-passable-for-now, carried
# openly. Default mode is build + flag + continue; this file is where
# the flags live until a strengthening pass.
#
# Items are added freely and marked RESOLVED (with what resolved them —
# a commit, a run, a correction) when fixed. The list reflects CURRENT
# weaknesses; it does not grow monotonically. Resolved items may be
# pruned once noted in a gate digest.
#
# Format: - [W-<n>] <one line> | raised <beat> | risk if left |
#   STATUS: OPEN | RESOLVED (<what resolved it>)
#
(empty)
